import essentials


screen.update(True)

def main():
    running = True
    while running:
        screen.draw_string(0, 0, "Nothing here yet.")
        screen.update()
        for get_events:
            if quit_event:
                running = False
            elif key_down_event:
                if special_key:
                    running = False

main()