import essentials

def main():
    f = 0
    running = True
    while running:
        f += 1
        screen.clear()
        screen.draw_aai(0, 0, "disk/experimental/atleastimnotbrian.aai", (f // 4) % 32)
        screen.update()
        for get_events:
            if quit_event:
                running = False

main()