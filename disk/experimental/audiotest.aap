import essentials

def main():
    running = True
    audio.play_aam("disk/experimental/test.aam")
    while running:
        screen.clear()
        audio.update_all_sequences()
        screen.update()
        for get_events:
            if quit_event:
                running = False

main()